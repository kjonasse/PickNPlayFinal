--drops database if exists
DROP DATABASE IF EXISTS pickNplaydev;

--drops database if exists
DROP DATABASE IF EXISTS pickNplaytest;

--drops database if exists
DROP DATABASE IF EXISTS pickNplay;

--creates the DATABASE
CREATE DATABASE pickNplaydev

--creates the DATABASE
CREATE DATABASE pickNplaytest

--creates the DATABASE
CREATE DATABASE pickNplay
